# 合并型工作队列

后台刷新、索引重建等任务常被同一业务键重复触发。`Queue` 用 `pending` 集合保证一个键在等待期间只占一个槽位，并用有界 FIFO 提供背压；任务出队后该键可再次入队。

```go
q := coalescing_work_queue.New(100)
if !q.Push("tenant:42") {
    // 已在队列中，或队列已满
}
key, ok := q.Pop()
```

队列方法受互斥锁保护，可被多个生产者和消费者调用。它只合并“尚未出队”的任务；若要覆盖执行中的重复触发，还需额外维护 in-flight 状态。

