# 用对冲请求准入降低长尾延迟

对冲请求会在原请求迟迟未返回时向另一副本发送相同请求，以额外流量换取更低的长尾延迟。若没有准入限制，依赖变慢时对冲流量反而可能形成放大器。

示例用 `Gate` 为每次请求设置最早对冲间隔和最大对冲次数。调用方在定时器触发后调用 `ShouldHedge`，只有达到时间门槛且仍有配额时才发送副本请求。实践中还应只对幂等操作启用，并结合全局并发上限、取消剩余请求和对冲命中率指标。

```bash
go test ./daily/2026/07/22/hedged_request_gate
```
